import React from "react";

export default function Settings() {

    return React.createElement(
        "div",
        null,

        React.createElement(
            "h1",
            null,
            "Settings"
        ),

        React.createElement(
            "p",
            null,
            "Settings page."
        )
    );
}